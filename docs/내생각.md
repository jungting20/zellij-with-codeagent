

1. agent planner이 어떤 pane을 띄워줄지 결정
2. 일단 client 프로젝트 일 경우
    6분할? 
    코딩 pane(사실상 두뇌 역할) , 크롬네트워크 창 목록 pane
    로컬 실행 server pane, 브라우저 pane(pane 이름은 agent 브라우저가 보고있는 url 주소)
3. 흐름 모든 오류는 pane 레지스트리 관리용 pane으로 전송
    - pane 레지스트리 관리용 pane 실행
    - 네트워크 400 또는 500 오류 감지 -> 코딩 pane으로 어떤 api가 났는지 보고 
    - 코딩 pane는 어떤 컴포넌트에서 발생했는지 확인 
    - 확인이 끝나면 브라우저 pane으로 보내서 버튼 클릭 및 실제 현상 재현
    - 사용자에게 보고 후 코딩 pane으로 전송
